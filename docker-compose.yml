x-ebookarr-port: &ebookarr_port "19191"

networks:
  # Shared internal network so ebookarr (which runs in tailscale's network
  # namespace via network_mode: service:tailscale) can reach flaresolverr.
  internal:
  # Shared bridge with projects/trawl (FlareSolverr replacement) so ebookarr
  # can also reach trawl:8191 (Anna's Archive path).
  trawl:
    external: true
    name: trawl

services:
  tailscale:
    image: tailscale/tailscale:stable
    hostname: itai-books
    entrypoint: ["/bin/sh", "/config/bootstrap.sh"]
    env_file:
      - .tailscale.env
    environment:
      FUNNEL_TARGET_PORT: *ebookarr_port
      TS_AUTH_ONCE: "true"
      TS_HOSTNAME: itai-books
      TS_STATE_DIR: /var/lib/tailscale
    volumes:
      - tailscale-state:/var/lib/tailscale
      - ./tailscale:/config:ro
    networks:
      - default
      - internal
      - trawl
    restart: unless-stopped

  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    restart: unless-stopped
    environment:
      - LOG_LEVEL=info
    networks:
      - internal

  ebookarr:
    build:
      context: .
    depends_on:
      tailscale:
        condition: service_started
      flaresolverr:
        condition: service_started
    env_file:
      - .env
    environment:
      DOWNLOAD_DIR: /data/downloads
      LOG_PATH: /data/books.log
      PORT: *ebookarr_port
      API_KEYS_FILE: /app/api_keys.json
    volumes:
      - ebookarr-data:/data
      - ./api_keys.json:/app/api_keys.json:rw
    network_mode: service:tailscale
    restart: unless-stopped

volumes:
  tailscale-state:
  ebookarr-data:
